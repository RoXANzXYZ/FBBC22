<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Footer Preview</title>
    
    <style>
        :root {
            --black: #212529;
            --gray: #495057;
            --gold: #ffc42c;
            --font-body: 'Roboto', sans-serif;
        }

        /* Basic body reset */
        body {
            margin: 0;
            padding: 0;
            font-family: var(--font-body);
        }

        /* The styles for your footer */
        .site-footer {
            background-color: var(--black);
            color: #ced4da; /* A light gray for the text */
            padding: 60px 20px;
            text-align: center;
        }
        .site-footer p {
            margin: 0 0 1rem 0;
            color: #adb5bd; /* A slightly darker gray for paragraphs */
            font-size: 1rem;
        }
        .site-footer a {
            color: var(--gold); /* The gold color for the link */
            text-decoration: none;
            font-weight: 700;
        }
        .site-footer a:hover {
            text-decoration: underline;
        }
        .copyright {
             margin-top: 2rem;
             font-size: 0.9rem;
             color: var(--gray);
        }
    </style>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">

</head>
<body>

    <footer class="site-footer">
         <p>Having trouble? Call or text us at <a href="tel:513-334-7945">513-334-7945</a>.</p>
         <p class="copyright">© 2025 Centerville Fit Body Bootcamp. All Rights Reserved.</p>
    </footer>

</body>
</html>
